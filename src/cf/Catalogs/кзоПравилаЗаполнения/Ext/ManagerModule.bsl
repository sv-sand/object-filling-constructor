
#Область ПрограммныйИНтерфейс

// Формирует наименование объекта справочника
//
// Параметры:
//  Объект	 - СправочникОбъект.кзоПравилаЗаполнения	 - Объект справоничка или формы
// 
// Возвращаемое значение:
//  Строка - Наименование
//
Функция СформироватьНаименование(Объект) Экспорт	
	
	Текст = Строка(Объект.ОбъектМетаданных);
	Если ЗначениеЗаполнено(Объект.ВидСобытия) Тогда
		Текст = Текст + " / " + Объект.ВидСобытия;	
	КонецЕсли;
	
	Возврат Текст;
	
КонецФункции

#КонецОбласти
